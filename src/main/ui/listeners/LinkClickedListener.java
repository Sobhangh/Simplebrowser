package ui.listeners;

public interface LinkClickedListener {
    /**
     * Method the registered listeners implement to handle when a link is being clicked
     * @param link
     */
    void linkClicked(String link);
}
